import world_common;
import sampling_core;

public static const uint PATH_DEBUG_VIEW = 13u;
public static const uint PATH_TEMPORAL_DEBUG_VIEW = 14u;
public static const uint PATH_REPLAY_DEBUG_VIEW = 15u;
public static const uint PATH_TEMPORAL_MERGE_DEBUG_VIEW = 16u;
public static const uint PATH_SPATIAL_DEBUG_VIEW = 17u;
public static const uint PATH_SPATIAL_POLICY_DEBUG_VIEW = 18u;
public static const uint PATH_RECONNECTION_DEBUG_VIEW = 19u;
public static const uint PATH_SHIFTED_RADIANCE_DEBUG_VIEW = 20u;
public static const uint PATH_SPATIAL_DIAGNOSTIC_STRICT_PAIR_CURSOR = 9u;
public static const uint PATH_SPATIAL_DIAGNOSTIC_LIMITED_ADMITTED = 10u;
public static const uint PATH_SPATIAL_DIAGNOSTIC_TOPOLOGY_RESCUED = 11u;
public static const uint PATH_SPATIAL_DIAGNOSTIC_RESCUED_PAIR_CURSOR = 12u;
public static const uint PATH_SPATIAL_DIAGNOSTIC_PAIR_CAPACITY = 4096u;
public static const uint PATH_SHIFT_DIAGNOSTIC_SELECTION_ELIGIBLE = 15u;
public static const uint PATH_SHIFT_DIAGNOSTIC_SOURCE_SELECTED = 16u;
public static const uint PATH_SHIFT_DIAGNOSTIC_SCRATCH_WRITTEN = 17u;
public static const uint PATH_SHIFT_DIAGNOSTIC_SCRATCH_SELECTED = 18u;
public static const uint PATH_SHIFT_DIAGNOSTIC_SCRATCH_INVALID = 19u;
public static const uint PATH_SHIFT_DIAGNOSTIC_PAIR_CURSOR = 20u;
public static const uint PATH_MAPPING_REPLAY_ELIGIBLE = 21u;
public static const uint PATH_MAPPING_REPLAY_ACCEPTED = 22u;
public static const uint PATH_MAPPING_REPLAY_ABI_REJECT = 23u;
public static const uint PATH_MAPPING_REPLAY_SOURCE_REJECT = 24u;
public static const uint PATH_MAPPING_REPLAY_RECEIVER_REJECT = 25u;
public static const uint PATH_MAPPING_REPLAY_GEOMETRY_REJECT = 26u;
public static const uint PATH_MAPPING_REPLAY_PDF_REJECT = 27u;
public static const uint PATH_MAPPING_REPLAY_VISIBILITY_REJECT = 28u;
public static const uint PATH_MAPPING_REPLAY_RADIANCE_REJECT = 29u;
public static const uint PATH_SHIFT_SOURCE_ROOT_WRITTEN = 30u;
public static const uint PATH_SHIFT_SOURCE_ROOT_INVALID = 31u;
public static const uint PATH_MAPPING_REPLAY_SOURCE_ROOT_REJECT = 32u;
public static const uint PATH_CROSS_FRAME_ATTEMPTED = 33u;
public static const uint PATH_CROSS_FRAME_RECEIVER_REPROJECTION_REJECT = 34u;
public static const uint PATH_CROSS_FRAME_MAPPED_EMPTY = 35u;
public static const uint PATH_CROSS_FRAME_ELIGIBLE = 36u;
public static const uint PATH_CROSS_FRAME_ACCEPTED = 37u;
public static const uint PATH_CROSS_FRAME_ABI_REJECT = 38u;
public static const uint PATH_CROSS_FRAME_SOURCE_ROOT_REJECT = 39u;
public static const uint PATH_CROSS_FRAME_SOURCE_REPROJECTION_REJECT = 40u;
public static const uint PATH_CROSS_FRAME_SOURCE_REPLAY_REJECT = 41u;
public static const uint PATH_CROSS_FRAME_RECEIVER_SURFACE_REJECT = 42u;
public static const uint PATH_CROSS_FRAME_GEOMETRY_REJECT = 43u;
public static const uint PATH_CROSS_FRAME_PDF_REJECT = 44u;
public static const uint PATH_CROSS_FRAME_VISIBILITY_REJECT = 45u;
public static const uint PATH_CROSS_FRAME_RADIANCE_REJECT = 46u;
public static const uint PATH_CROSS_FRAME_RECEIVER_SAMPLE_REJECT = 47u;
public static const uint PATH_CROSS_FRAME_RECEIVER_EDGE_REJECT = 48u;
public static const uint PATH_CROSS_FRAME_RECEIVER_TOPOLOGY_REJECT = 49u;
public static const uint PATH_CROSS_FRAME_RECEIVER_DEPTH_REJECT = 50u;
public static const uint PATH_CROSS_FRAME_RECEIVER_TRANSPORT_REJECT = 51u;
public static const uint PATH_CROSS_FRAME_RECEIVER_FOOTPRINT_REJECT = 52u;
public static const uint PATH_CROSS_FRAME_SAMPLE_RESCUE_ELIGIBLE = 53u;
public static const uint PATH_CROSS_FRAME_SAMPLE_RESCUED = 54u;
public static const uint PATH_CROSS_FRAME_SAMPLE_RESCUE_EDGE_REJECT = 55u;
public static const uint PATH_CROSS_FRAME_SAMPLE_RESCUE_TOPOLOGY_REJECT = 56u;
public static const uint PATH_CROSS_FRAME_SAMPLE_RESCUE_DEPTH_REJECT = 57u;
public static const uint PATH_CROSS_FRAME_SAMPLE_RESCUE_TRANSPORT_REJECT = 58u;
public static const uint PATH_CROSS_FRAME_SAMPLE_RESCUE_FOOTPRINT_REJECT = 59u;
public static const uint PATH_CROSS_FRAME_EDGE_BREAKDOWN_ELIGIBLE = 60u;
public static const uint PATH_CROSS_FRAME_EDGE_MISSING_VALID = 61u;
public static const uint PATH_CROSS_FRAME_EDGE_DEPTH_REJECT = 62u;
public static const uint PATH_CROSS_FRAME_EDGE_EVENT_REJECT = 63u;
public static const uint PATH_CROSS_FRAME_EDGE_MAPPING_REJECT = 64u;
public static const uint PATH_CROSS_FRAME_EDGE_PDF_REJECT = 65u;
public static const uint PATH_CROSS_FRAME_EDGE_FINITE_REJECT = 66u;
public static const uint PATH_RECEIVER_GUIDE_ATTEMPTED = 67u;
public static const uint PATH_RECEIVER_GUIDE_INVALID = 68u;
public static const uint PATH_RECEIVER_GUIDE_NO_STORED_EDGE = 69u;
public static const uint PATH_RECEIVER_GUIDE_STORED_ELIGIBLE = 70u;
public static const uint PATH_RECEIVER_GUIDE_ACCEPTED = 71u;
public static const uint PATH_RECEIVER_GUIDE_MASS_MISMATCH = 72u;
public static const uint PATH_RECEIVER_GUIDE_THROUGHPUT_MISMATCH = 73u;
public static const uint PATH_RECEIVER_GUIDE_PDF_MISMATCH = 74u;
public static const uint PATH_GUIDE_REMAP_ELIGIBLE = 75u;
public static const uint PATH_GUIDE_REMAP_READY = 76u;
public static const uint PATH_GUIDE_REMAP_GEOMETRY_REJECT = 77u;
public static const uint PATH_GUIDE_REMAP_PDF_REJECT = 78u;
public static const uint PATH_GUIDE_REMAP_THROUGHPUT_REJECT = 79u;
public static const uint PATH_GUIDE_VISIBILITY_ELIGIBLE = 80u;
public static const uint PATH_GUIDE_VISIBILITY_CLEAR = 81u;
public static const uint PATH_GUIDE_VISIBILITY_TINTED = 82u;
public static const uint PATH_GUIDE_VISIBILITY_OCCLUDED = 83u;
public static const uint PATH_GUIDE_VISIBILITY_INVALID = 84u;
public static const uint PATH_GUIDE_TARGET_ELIGIBLE = 85u;
public static const uint PATH_GUIDE_TARGET_POSITIVE = 86u;
public static const uint PATH_GUIDE_TARGET_ZERO = 87u;
public static const uint PATH_GUIDE_TARGET_INVALID = 88u;
public static const uint PATH_GUIDE_WEIGHT_ELIGIBLE = 89u;
public static const uint PATH_GUIDE_WEIGHT_POSITIVE = 90u;
public static const uint PATH_GUIDE_WEIGHT_ZERO = 91u;
public static const uint PATH_GUIDE_WEIGHT_INVALID = 92u;
public static const uint PATH_GUIDE_SELECTION_ELIGIBLE = 93u;
public static const uint PATH_GUIDE_SELECTION_POSITIVE = 94u;
public static const uint PATH_GUIDE_SELECTION_ZERO = 95u;
public static const uint PATH_GUIDE_SELECTION_CURRENT_REJECT = 96u;
public static const uint PATH_GUIDE_SELECTION_PROBABILITY_HIGH_REJECT = 97u;
public static const uint PATH_GUIDE_SELECTION_ARITHMETIC_REJECT = 98u;
public static const uint PATH_GUIDE_STABLE_SELECTION_ELIGIBLE = 99u;
public static const uint PATH_GUIDE_STABLE_SELECTION_POSITIVE = 100u;
public static const uint PATH_GUIDE_STABLE_SELECTION_ZERO = 101u;
public static const uint PATH_GUIDE_STABLE_SELECTION_INVALID = 102u;
public static const uint PATH_GUIDE_STABLE_BERNOULLI_ELIGIBLE = 103u;
public static const uint PATH_GUIDE_STABLE_BERNOULLI_SELECTED = 104u;
public static const uint PATH_GUIDE_STABLE_BERNOULLI_RETAINED = 105u;
public static const uint PATH_GUIDE_STABLE_BERNOULLI_INVALID = 106u;
public static const uint PATH_GUIDE_POST_SELECTION_ELIGIBLE = 107u;
public static const uint PATH_GUIDE_POST_SELECTION_SELECTED_READY = 108u;
public static const uint PATH_GUIDE_POST_SELECTION_RETAINED_READY = 109u;
public static const uint PATH_GUIDE_POST_SELECTION_EMPTY = 110u;
public static const uint PATH_GUIDE_POST_SELECTION_SAMPLE_REJECT = 111u;
public static const uint PATH_GUIDE_POST_SELECTION_ARITHMETIC_REJECT = 112u;
public static const uint PATH_GUIDE_SAMPLE_COPY_ELIGIBLE = 113u;
public static const uint PATH_GUIDE_SAMPLE_COPY_SELECTED = 114u;
public static const uint PATH_GUIDE_SAMPLE_COPY_RETAINED = 115u;
public static const uint PATH_GUIDE_SAMPLE_COPY_EMPTY = 116u;
public static const uint PATH_GUIDE_SAMPLE_COPY_METADATA_REJECT = 117u;
public static const uint PATH_GUIDE_SAMPLE_COPY_ARITHMETIC_REJECT = 118u;
public static const uint PATH_GUIDE_SCRATCH_STORAGE_ELIGIBLE = 119u;
public static const uint PATH_GUIDE_SCRATCH_STORAGE_SELECTED = 120u;
public static const uint PATH_GUIDE_SCRATCH_STORAGE_RETAINED = 121u;
public static const uint PATH_GUIDE_SCRATCH_STORAGE_EMPTY = 122u;
public static const uint PATH_GUIDE_SCRATCH_STORAGE_METADATA_REJECT = 123u;
public static const uint PATH_GUIDE_SCRATCH_STORAGE_ARITHMETIC_REJECT = 124u;
public static const uint PATH_GUIDE_SCRATCH_STORAGE_INVALID = 125u;
public static const uint PATH_GUIDE_ROOT_STORAGE_ELIGIBLE = 126u;
public static const uint PATH_GUIDE_ROOT_STORAGE_SELECTED_READY = 127u;
public static const uint PATH_GUIDE_ROOT_STORAGE_RETAINED_READY = 128u;
public static const uint PATH_GUIDE_ROOT_STORAGE_MISSING = 129u;
public static const uint PATH_GUIDE_ROOT_STORAGE_METADATA_REJECT = 130u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_ATTEMPTED = 131u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_LIFECYCLE_REJECT = 132u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_RECEIVER_REPROJECTION_REJECT = 133u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_EMPTY = 134u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_METADATA_REJECT = 135u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_RECEIVER_SURFACE_REJECT = 136u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_SOURCE_REPROJECTION_REJECT = 137u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_SOURCE_SURFACE_REJECT = 138u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_SOURCE_REPLAY_REJECT = 139u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_SELECTED_ACCEPTED = 140u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_RETAINED_ACCEPTED = 141u;
public static const uint PATH_GUIDE_PREVIOUS_REMAP_ELIGIBLE = 142u;
public static const uint PATH_GUIDE_PREVIOUS_REMAP_RECEIVER_GUIDE_REJECT = 143u;
public static const uint PATH_GUIDE_PREVIOUS_REMAP_READY = 144u;
public static const uint PATH_GUIDE_PREVIOUS_REMAP_GEOMETRY_REJECT = 145u;
public static const uint PATH_GUIDE_PREVIOUS_REMAP_PDF_REJECT = 146u;
public static const uint PATH_GUIDE_PREVIOUS_REMAP_THROUGHPUT_REJECT = 147u;
public static const uint PATH_GUIDE_PREVIOUS_VISIBILITY_ELIGIBLE = 148u;
public static const uint PATH_GUIDE_PREVIOUS_VISIBILITY_CLEAR = 149u;
public static const uint PATH_GUIDE_PREVIOUS_VISIBILITY_TINTED = 150u;
public static const uint PATH_GUIDE_PREVIOUS_VISIBILITY_OCCLUDED = 151u;
public static const uint PATH_GUIDE_PREVIOUS_VISIBILITY_INVALID = 152u;
public static const uint PATH_GUIDE_PREVIOUS_TARGET_ELIGIBLE = 153u;
public static const uint PATH_GUIDE_PREVIOUS_TARGET_POSITIVE = 154u;
public static const uint PATH_GUIDE_PREVIOUS_TARGET_ZERO = 155u;
public static const uint PATH_GUIDE_PREVIOUS_TARGET_INVALID = 156u;
public static const uint PATH_GUIDE_PREVIOUS_STORED_METADATA_ELIGIBLE = 157u;
public static const uint PATH_GUIDE_PREVIOUS_STORED_TARGET_MATCH = 158u;
public static const uint PATH_GUIDE_PREVIOUS_STORED_TARGET_MISMATCH = 159u;
public static const uint PATH_GUIDE_PREVIOUS_STORED_TARGET_INVALID = 160u;
public static const uint PATH_GUIDE_PREVIOUS_STORED_THROUGHPUT_MATCH = 161u;
public static const uint PATH_GUIDE_PREVIOUS_STORED_THROUGHPUT_MISMATCH = 162u;
public static const uint PATH_GUIDE_PREVIOUS_STORED_THROUGHPUT_INVALID = 163u;
public static const uint PATH_GUIDE_PREVIOUS_WEIGHT_ELIGIBLE = 164u;
public static const uint PATH_GUIDE_PREVIOUS_WEIGHT_ZERO = 165u;
public static const uint PATH_GUIDE_PREVIOUS_WEIGHT_POSITIVE = 166u;
public static const uint PATH_GUIDE_PREVIOUS_WEIGHT_INVALID = 167u;
public static const uint PATH_GUIDE_PREVIOUS_POST_WEIGHT_ELIGIBLE = 168u;
public static const uint PATH_GUIDE_PREVIOUS_POST_WEIGHT_NEXT_VALID = 169u;
public static const uint PATH_GUIDE_PREVIOUS_POST_WEIGHT_NEXT_INVALID = 170u;
public static const uint PATH_GUIDE_PREVIOUS_POST_WEIGHT_CURRENT_VALID = 171u;
public static const uint PATH_GUIDE_PREVIOUS_POST_WEIGHT_CURRENT_INVALID = 172u;
public static const uint PATH_GUIDE_PREVIOUS_POST_WEIGHT_SOURCE_VALID = 173u;
public static const uint PATH_GUIDE_PREVIOUS_POST_WEIGHT_SOURCE_INVALID = 174u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_PROBABILITY_ELIGIBLE = 175u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_DENOMINATOR_VALID = 176u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_DENOMINATOR_INVALID = 177u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_PROBABILITY_VALID = 178u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_PROBABILITY_INVALID = 179u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_CURRENT_DENOMINATOR_VALID = 180u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_CURRENT_DENOMINATOR_INVALID = 181u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_SOURCE_DENOMINATOR_VALID = 182u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_SOURCE_DENOMINATOR_INVALID = 183u;
public static const uint PATH_GUIDE_PREVIOUS_SELECTION_PROBABILITY_HIGH_REJECT = 184u;
public static const uint PATH_GUIDE_PREVIOUS_STABLE_SELECTION_ELIGIBLE = 185u;
public static const uint PATH_GUIDE_PREVIOUS_STABLE_SELECTION_POSITIVE = 186u;
public static const uint PATH_GUIDE_PREVIOUS_STABLE_SELECTION_ZERO = 187u;
public static const uint PATH_GUIDE_PREVIOUS_STABLE_SELECTION_INVALID = 188u;
public static const uint PATH_GUIDE_PREVIOUS_STABLE_BERNOULLI_ELIGIBLE = 189u;
public static const uint PATH_GUIDE_PREVIOUS_STABLE_BERNOULLI_SELECTED = 190u;
public static const uint PATH_GUIDE_PREVIOUS_STABLE_BERNOULLI_RETAINED = 191u;
public static const uint PATH_GUIDE_PREVIOUS_STABLE_BERNOULLI_INVALID = 192u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_ELIGIBLE = 193u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_SELECTED_FINAL_VALID = 194u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_SELECTED_FINAL_INVALID = 195u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_RETAINED_FINAL_VALID = 196u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_RETAINED_FINAL_INVALID = 197u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_SELECTED_PAYLOAD_VALID = 198u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_SELECTED_PAYLOAD_INVALID = 199u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_RETAINED_PAYLOAD_VALID = 200u;
public static const uint PATH_GUIDE_PREVIOUS_BRANCH_RETAINED_PAYLOAD_INVALID = 201u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_ELIGIBLE = 202u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_SELECTED_REWRITE_READY = 203u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_SELECTED_REWRITE_REJECT = 204u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_SELECTED_PRESERVE_READY = 205u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_SELECTED_PRESERVE_REJECT = 206u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_RETAINED_PRESERVE_READY = 207u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_RETAINED_PRESERVE_REJECT = 208u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_WEIGHTS_READY = 209u;
public static const uint PATH_GUIDE_PREVIOUS_LANE_WEIGHTS_REJECT = 210u;
public static const uint PATH_GUIDE_PREVIOUS_RECORD_ELIGIBLE = 211u;
public static const uint PATH_GUIDE_PREVIOUS_RECORD_SELECTED_READY = 212u;
public static const uint PATH_GUIDE_PREVIOUS_RECORD_SELECTED_REJECT = 213u;
public static const uint PATH_GUIDE_PREVIOUS_RECORD_RETAINED_READY = 214u;
public static const uint PATH_GUIDE_PREVIOUS_RECORD_RETAINED_REJECT = 215u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_ELIGIBLE = 216u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_SELECTED_READY = 217u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_SELECTED_REJECT = 218u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_RETAINED_READY = 219u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_RETAINED_REJECT = 220u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_ROOT_CAPTURE_REJECT = 221u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_ROOT_CHAIN_REJECT = 222u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_ROOT_IDENTITY_REJECT = 223u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_REPLAY_ELIGIBLE = 224u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_REPLAY_SELECTED_ACCEPTED = 225u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_REPLAY_SELECTED_REJECT = 226u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_REPLAY_RETAINED_ACCEPTED = 227u;
public static const uint PATH_GUIDE_PREVIOUS_PAIR_REPLAY_RETAINED_REJECT = 228u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_ATTEMPTED = 229u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_EMPTY = 230u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_METADATA_REJECT = 231u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_RECEIVER_REPROJECTION_REJECT = 232u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_RECEIVER_SURFACE_REJECT = 233u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_SOURCE_REPROJECTION_REJECT = 234u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_SOURCE_SURFACE_REJECT = 235u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_SOURCE_REPLAY_REJECT = 236u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_SELECTED_ACCEPTED = 237u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_RETAINED_ACCEPTED = 238u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_REPLAY_DELTA = 239u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_WRITE_ELIGIBLE = 240u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_WRITE_COMPLETED = 241u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_CAPTURE_CURSOR = 242u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_ATTEMPTED = 243u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_METADATA_REJECT = 244u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_RESERVOIR_MATCH = 245u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_RESERVOIR_MISMATCH = 246u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_ROOT_MATCH = 247u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_ROOT_MISMATCH = 248u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_SELECTED_ACCEPTED = 249u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_RETAINED_ACCEPTED = 250u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_PAIR_REJECT = 251u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_DELTA = 252u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_TAG_WRITE_ELIGIBLE = 253u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_TAG_SELECTED_WRITTEN = 254u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_TAG_RETAINED_WRITTEN = 255u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_TAG_REPLAY_EMPTY = 256u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_TAG_REPLAY_METADATA_REJECT = 257u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_TAG_REPLAY_SELECTED_ADMITTED = 258u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_TAG_REPLAY_RETAINED_ADMITTED = 259u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_TAG_REPLAY_DELTA = 260u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_ATTEMPTED = 261u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_EMPTY = 262u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_FUTURE_REJECT = 263u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_GENERATION_REJECT = 264u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_MAPPING_REJECT = 265u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_CURRENT = 266u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_AGE_ONE = 267u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_AGE_TWO = 268u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_AGE_THREE = 269u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_EXPIRED = 270u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_IDENTITY_LIVE = 271u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_MAPPED_LIVE = 272u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_DELTA = 273u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_ATTEMPTED = 274u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_EMPTY = 275u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_METADATA_REJECT = 276u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_CURRENT_SKIP = 277u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_EXPIRED = 278u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_PROVENANCE_REJECT = 279u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_AGE_ONE_ELIGIBLE = 280u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_AGE_TWO_ELIGIBLE = 281u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_AGE_THREE_ELIGIBLE = 282u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_IDENTITY_ACCEPTED = 283u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_IDENTITY_REJECT = 284u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_MAPPED_ROOT_ACCEPTED = 285u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_MAPPED_ROOT_REJECT = 286u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_DELTA = 287u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_AGE_ONE_ACCEPTED = 288u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_AGE_TWO_ACCEPTED = 289u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_AGE_THREE_ACCEPTED = 290u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_AGE_DELTA = 291u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_ELIGIBLE = 292u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_CLIP_REJECT = 293u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_BOUNDS_REJECT = 294u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_SURFACE_REJECT = 295u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_AGE_ONE_ADMITTED = 296u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_AGE_TWO_ADMITTED = 297u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_AGE_THREE_ADMITTED = 298u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_IDENTITY_ADMITTED = 299u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_MAPPED_ADMITTED = 300u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_DELTA = 301u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_ELIGIBLE = 302u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_GUIDE_REJECT = 303u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_EDGE_REJECT = 304u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_GEOMETRY_REJECT = 305u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_PDF_REJECT = 306u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_THROUGHPUT_REJECT = 307u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_AGE_ONE_READY = 308u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_AGE_TWO_READY = 309u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_AGE_THREE_READY = 310u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_IDENTITY_READY = 311u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_MAPPED_READY = 312u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_REMAP_DELTA = 313u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_VISIBILITY_ELIGIBLE = 314u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_VISIBILITY_CLEAR = 315u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_VISIBILITY_TINTED = 316u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_VISIBILITY_OCCLUDED = 317u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_VISIBILITY_INVALID = 318u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_ELIGIBLE = 319u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_POSITIVE = 320u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_ZERO = 321u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_INVALID = 322u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_AGE_ONE_READY = 323u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_AGE_TWO_READY = 324u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_AGE_THREE_READY = 325u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_IDENTITY_READY = 326u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_MAPPED_READY = 327u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_VISIBILITY_DELTA = 328u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_TARGET_DELTA = 329u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_ELIGIBLE = 330u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_POSITIVE = 331u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_ZERO = 332u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_INVALID = 333u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_AGE_ONE_READY = 334u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_AGE_TWO_READY = 335u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_AGE_THREE_READY = 336u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_IDENTITY_READY = 337u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_MAPPED_READY = 338u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_COUNT_UNCAPPED = 339u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_COUNT_CAPPED = 340u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_WEIGHT_DELTA = 341u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_ELIGIBLE = 342u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_CURRENT_REJECT = 343u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_PROBABILITY_ZERO = 344u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_PROBABILITY_OPEN = 345u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_PROBABILITY_ONE = 346u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_PROBABILITY_INVALID = 347u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_AGE_ONE_READY = 348u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_AGE_TWO_READY = 349u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_AGE_THREE_READY = 350u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_IDENTITY_READY = 351u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_MAPPED_READY = 352u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_CURRENT_ZERO = 353u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_CURRENT_POSITIVE = 354u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_SELECTION_DELTA = 355u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_ELIGIBLE = 356u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_SELECTED = 357u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_RETAINED = 358u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_INVALID = 359u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_PROBABILITY_ZERO = 360u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_PROBABILITY_OPEN = 361u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_PROBABILITY_ONE = 362u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_ZERO_VIOLATION = 363u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_ONE_VIOLATION = 364u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_AGE_ONE_READY = 365u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_AGE_TWO_READY = 366u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_AGE_THREE_READY = 367u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_IDENTITY_READY = 368u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_MAPPED_READY = 369u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_BERNOULLI_DELTA = 370u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_ELIGIBLE = 371u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_CURRENT_REJECT = 372u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_NEXT_INVALID = 373u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_SELECTED_READY = 374u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_SELECTED_TARGET_REJECT = 375u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_SELECTED_FINAL_REJECT = 376u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_RETAINED_READY = 377u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_RETAINED_TARGET_REJECT = 378u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_RETAINED_FINAL_REJECT = 379u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_EMPTY_READY = 380u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_WEIGHT_UNCAPPED = 381u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_WEIGHT_CAPPED = 382u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_COUNT_UNCAPPED = 383u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_COUNT_CAPPED = 384u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_AGE_ONE_READY = 385u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_AGE_TWO_READY = 386u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_AGE_THREE_READY = 387u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_IDENTITY_READY = 388u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_MAPPED_READY = 389u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_POST_SELECTION_DELTA = 390u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_ELIGIBLE = 391u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_SELECTED_READY = 392u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_SELECTED_REJECT = 393u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_RETAINED_READY = 394u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_RETAINED_REJECT = 395u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_EMPTY_READY = 396u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_SELECTED_REWRITE_READY = 397u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_SELECTED_REWRITE_REJECT = 398u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_SELECTED_PRESERVE_READY = 399u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_SELECTED_PRESERVE_REJECT = 400u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_RETAINED_PRESERVE_READY = 401u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_RETAINED_PRESERVE_REJECT = 402u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_WEIGHTS_READY = 403u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_WEIGHTS_REJECT = 404u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_AGE_ONE_READY = 405u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_AGE_TWO_READY = 406u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_AGE_THREE_READY = 407u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_IDENTITY_SOURCE_READY = 408u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_MAPPED_SOURCE_READY = 409u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_RECORD_DELTA = 410u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_ELIGIBLE = 411u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SELECTED_READY = 412u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SELECTED_REJECT = 413u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_RETAINED_READY = 414u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_RETAINED_REJECT = 415u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_EMPTY_READY = 416u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SOURCE_READY = 417u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SOURCE_CLIP_REJECT = 418u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SOURCE_BOUNDS_REJECT = 419u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SOURCE_SURFACE_REJECT = 420u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SELECTED_KEY_READY = 421u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SELECTED_KEY_REJECT = 422u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SELECTED_ROOT_READY = 423u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_SELECTED_ROOT_REJECT = 424u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_RETAINED_ROOT_READY = 425u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_RETAINED_ROOT_REJECT = 426u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_ROOT_CHAIN_REJECT = 427u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_ROOT_IDENTITY_REJECT = 428u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_AGE_ONE_READY = 429u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_AGE_TWO_READY = 430u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_AGE_THREE_READY = 431u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_IDENTITY_SOURCE_READY = 432u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_MAPPED_SOURCE_READY = 433u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_DELTA = 434u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_ELIGIBLE = 435u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_SELECTED_ACCEPTED = 436u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_SELECTED_REJECT = 437u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_RETAINED_ACCEPTED = 438u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_RETAINED_REJECT = 439u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_AGE_ONE_ACCEPTED = 440u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_AGE_TWO_ACCEPTED = 441u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_AGE_THREE_ACCEPTED = 442u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_IDENTITY_SOURCE_ACCEPTED = 443u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_MAPPED_SOURCE_ACCEPTED = 444u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_ONE_SEGMENT_ACCEPTED = 445u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_TWO_SEGMENT_ACCEPTED = 446u;
public static const uint PATH_GUIDE_BRANCH_DIRECT_PAIR_REPLAY_DELTA = 447u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_WRITE_ELIGIBLE = 448u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_WRITE_COMPLETED = 449u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_CAPTURE_CURSOR = 450u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_ATTEMPTED = 451u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_METADATA_REJECT = 452u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_RESERVOIR_MATCH = 453u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_RESERVOIR_MISMATCH = 454u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_ROOT_MATCH = 455u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_ROOT_MISMATCH = 456u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_SELECTED_ACCEPTED = 457u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_RETAINED_ACCEPTED = 458u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_PAIR_REJECT = 459u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_AGE_ONE_ACCEPTED = 460u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_AGE_TWO_ACCEPTED = 461u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_AGE_THREE_ACCEPTED = 462u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_IDENTITY_SOURCE_ACCEPTED = 463u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_MAPPED_SOURCE_ACCEPTED = 464u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_ONE_SEGMENT_ACCEPTED = 465u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_TWO_SEGMENT_ACCEPTED = 466u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_DELTA = 467u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_CLAIM_ELIGIBLE = 468u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_CLAIM_WRITTEN = 469u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_VALIDATE_ATTEMPTED = 470u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_EMPTY = 471u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_UNIQUE = 472u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_COLLISION = 473u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_FAN_IN_TWO = 474u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_FAN_IN_THREE = 475u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_FAN_IN_FOUR_PLUS = 476u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_MAX_FAN_IN = 477u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_CLAIM_SUM = 478u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_METADATA_REJECT = 479u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_ACCEPTED = 480u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_SELECTED = 481u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_RETAINED = 482u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_AGE_ONE = 483u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_AGE_TWO = 484u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_AGE_THREE = 485u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_IDENTITY_SOURCE = 486u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_MAPPED_SOURCE = 487u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_ONE_SEGMENT = 488u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_WINNER_TWO_SEGMENT = 489u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_DELTA = 490u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_WRITE_ELIGIBLE = 491u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_WRITE_COMPLETED = 492u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_VALIDATE_ATTEMPTED = 493u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_EMPTY = 494u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_EMPTY_DIRTY_REJECT = 495u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_METADATA_REJECT = 496u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_RESERVOIR_MATCH = 497u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_RESERVOIR_MISMATCH = 498u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_ROOT_MATCH = 499u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_ROOT_MISMATCH = 500u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_PAIR_ACCEPTED = 501u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_PAIR_REJECT = 502u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_SELECTED_ACCEPTED = 503u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_RETAINED_ACCEPTED = 504u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_AGE_ONE_ACCEPTED = 505u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_AGE_TWO_ACCEPTED = 506u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_AGE_THREE_ACCEPTED = 507u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_IDENTITY_SOURCE_ACCEPTED = 508u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_MAPPED_SOURCE_ACCEPTED = 509u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_ONE_SEGMENT_ACCEPTED = 510u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_TWO_SEGMENT_ACCEPTED = 511u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_DELTA = 512u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_ATTEMPTED = 513u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_RECEIVER_REPROJECTION_REJECT = 514u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_EMPTY = 515u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_METADATA_REJECT = 516u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_RECEIVER_SURFACE_REJECT = 517u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_SOURCE_REPROJECTION_REJECT = 518u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_SOURCE_SURFACE_REJECT = 519u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_SOURCE_REPLAY_REJECT = 520u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_SELECTED_ACCEPTED = 521u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_RETAINED_ACCEPTED = 522u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_IDENTITY_SOURCE_ACCEPTED = 523u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_MAPPED_SOURCE_ACCEPTED = 524u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_ONE_SEGMENT_ACCEPTED = 525u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_TWO_SEGMENT_ACCEPTED = 526u;
public static const uint PATH_GUIDE_BRANCH_WINNER_REPLAY_DELTA = 527u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_REMAP_ELIGIBLE = 528u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_REMAP_GUIDE_REJECT = 529u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_REMAP_EDGE_REJECT = 530u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_REMAP_GEOMETRY_REJECT = 531u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_REMAP_PDF_REJECT = 532u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_REMAP_THROUGHPUT_REJECT = 533u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_REMAP_READY = 534u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_VISIBILITY_ELIGIBLE = 535u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_VISIBILITY_CLEAR = 536u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_VISIBILITY_TINTED = 537u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_VISIBILITY_OCCLUDED = 538u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_VISIBILITY_INVALID = 539u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_TARGET_ELIGIBLE = 540u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_TARGET_POSITIVE = 541u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_TARGET_ZERO = 542u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_TARGET_INVALID = 543u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_WEIGHT_ELIGIBLE = 544u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_WEIGHT_POSITIVE = 545u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_WEIGHT_ZERO = 546u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_WEIGHT_INVALID = 547u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_WEIGHT_COUNT_UNCAPPED = 548u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_WEIGHT_COUNT_CAPPED = 549u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_SELECTED_READY = 550u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_RETAINED_READY = 551u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_IDENTITY_SOURCE_READY = 552u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_MAPPED_SOURCE_READY = 553u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_ONE_SEGMENT_READY = 554u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_TWO_SEGMENT_READY = 555u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DIRECT_DELTA = 556u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_ELIGIBLE = 557u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_CURRENT_REJECT = 558u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_PROBABILITY_ZERO = 559u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_PROBABILITY_OPEN = 560u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_PROBABILITY_ONE = 561u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_PROBABILITY_INVALID = 562u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_CURRENT_ZERO = 563u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_CURRENT_POSITIVE = 564u;
public static const uint PATH_GUIDE_BRANCH_WINNER_SELECTION_DELTA = 565u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_ELIGIBLE = 566u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_SELECTED = 567u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_RETAINED = 568u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_INVALID = 569u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_PROBABILITY_ZERO = 570u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_PROBABILITY_OPEN = 571u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_PROBABILITY_ONE = 572u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_ZERO_VIOLATION = 573u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_ONE_VIOLATION = 574u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_PREVIOUS_SELECTED = 575u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_PREVIOUS_RETAINED = 576u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_IDENTITY_SOURCE = 577u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_MAPPED_SOURCE = 578u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_ONE_SEGMENT = 579u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_TWO_SEGMENT = 580u;
public static const uint PATH_GUIDE_BRANCH_WINNER_BERNOULLI_DELTA = 581u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_ELIGIBLE = 582u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_CURRENT_REJECT = 583u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_NEXT_INVALID = 584u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_SELECTED_READY = 585u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_SELECTED_TARGET_REJECT = 586u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_SELECTED_FINAL_REJECT = 587u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_RETAINED_READY = 588u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_RETAINED_TARGET_REJECT = 589u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_RETAINED_FINAL_REJECT = 590u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_EMPTY_READY = 591u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_WEIGHT_UNCAPPED = 592u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_WEIGHT_CAPPED = 593u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_COUNT_UNCAPPED = 594u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_COUNT_CAPPED = 595u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_PREVIOUS_SELECTED = 596u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_PREVIOUS_RETAINED = 597u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_IDENTITY_SOURCE = 598u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_MAPPED_SOURCE = 599u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_ONE_SEGMENT = 600u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_TWO_SEGMENT = 601u;
public static const uint PATH_GUIDE_BRANCH_WINNER_POST_SELECTION_DELTA = 602u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_ELIGIBLE = 603u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_SELECTED_READY = 604u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_SELECTED_REJECT = 605u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_RETAINED_READY = 606u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_RETAINED_REJECT = 607u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_EMPTY_READY = 608u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_SELECTED_REWRITE_READY = 609u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_SELECTED_REWRITE_REJECT = 610u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_SELECTED_PRESERVE_READY = 611u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_SELECTED_PRESERVE_REJECT = 612u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_RETAINED_PRESERVE_READY = 613u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_RETAINED_PRESERVE_REJECT = 614u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_WEIGHTS_READY = 615u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_WEIGHTS_REJECT = 616u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_METADATA_READY = 617u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_METADATA_REJECT = 618u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_SOURCE_KEY_READY = 619u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_SOURCE_KEY_REJECT = 620u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_PREVIOUS_SELECTED = 621u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_PREVIOUS_RETAINED = 622u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_IDENTITY_SOURCE = 623u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_MAPPED_SOURCE = 624u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_ONE_SEGMENT = 625u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_TWO_SEGMENT = 626u;
public static const uint PATH_GUIDE_BRANCH_WINNER_RECORD_DELTA = 627u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_ELIGIBLE = 628u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_SELECTED_READY = 629u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_SELECTED_REJECT = 630u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_RETAINED_READY = 631u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_RETAINED_REJECT = 632u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_EMPTY_READY = 633u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_SELECTED_KEY_READY = 634u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_SELECTED_KEY_REJECT = 635u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_SELECTED_ROOT_READY = 636u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_SELECTED_ROOT_REJECT = 637u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_RETAINED_ROOT_READY = 638u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_RETAINED_ROOT_REJECT = 639u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_ROOT_CHAIN_REJECT = 640u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_ROOT_IDENTITY_REJECT = 641u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_PREVIOUS_SELECTED = 642u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_PREVIOUS_RETAINED = 643u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_IDENTITY_SOURCE = 644u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_MAPPED_SOURCE = 645u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_ONE_SEGMENT = 646u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_TWO_SEGMENT = 647u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_DELTA = 648u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_ELIGIBLE = 649u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_SELECTED_ACCEPTED = 650u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_SELECTED_REJECT = 651u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_RETAINED_ACCEPTED = 652u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_RETAINED_REJECT = 653u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_PREVIOUS_SELECTED = 654u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_PREVIOUS_RETAINED = 655u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_IDENTITY_SOURCE = 656u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_MAPPED_SOURCE = 657u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_ONE_SEGMENT = 658u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_TWO_SEGMENT = 659u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_REPLAY_DELTA = 660u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_WRITE_ELIGIBLE = 661u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_CAPTURE_CURSOR = 662u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_WRITE_COMPLETED = 663u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_OVERFLOW = 664u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_VALIDATE_ATTEMPTED = 665u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_METADATA_REJECT = 666u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_RESERVOIR_MATCH = 667u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_RESERVOIR_MISMATCH = 668u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_ROOT_MATCH = 669u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_ROOT_MISMATCH = 670u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_PAIR_ACCEPTED = 671u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_PAIR_REJECT = 672u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_SELECTED_ACCEPTED = 673u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_RETAINED_ACCEPTED = 674u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_PREVIOUS_SELECTED = 675u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_PREVIOUS_RETAINED = 676u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_IDENTITY_SOURCE = 677u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_MAPPED_SOURCE = 678u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_ONE_SEGMENT = 679u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_TWO_SEGMENT = 680u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_DELTA = 681u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_WRITE_ELIGIBLE = 682u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_WRITE_COMPLETED = 683u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_VALIDATE_ATTEMPTED = 684u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_EMPTY = 685u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_METADATA_REJECT = 686u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_ACCEPTED = 687u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_SELECTED = 688u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_RETAINED = 689u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_PREVIOUS_SELECTED = 690u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_PREVIOUS_RETAINED = 691u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_IDENTITY_SOURCE = 692u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_MAPPED_SOURCE = 693u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_ONE_SEGMENT = 694u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_TWO_SEGMENT = 695u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_WRITE_ELIGIBLE = 696u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_WRITE_COMPLETED = 697u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_VALIDATE_ATTEMPTED = 698u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_METADATA_REJECT = 699u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_RESERVOIR_MATCH = 700u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_RESERVOIR_MISMATCH = 701u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_ROOT_MATCH = 702u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_ROOT_MISMATCH = 703u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_PAIR_ACCEPTED = 704u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_PAIR_REJECT = 705u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_SELECTED_ACCEPTED = 706u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_RETAINED_ACCEPTED = 707u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_PREVIOUS_SELECTED = 708u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_PREVIOUS_RETAINED = 709u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_IDENTITY_SOURCE = 710u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_MAPPED_SOURCE = 711u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_ONE_SEGMENT = 712u;
public static const uint PATH_GUIDE_BRANCH_WINNER_DENSE_PAYLOAD_TWO_SEGMENT = 713u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_ATTEMPTED = 714u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_NONE = 715u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_FRESH_ONLY = 716u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_AGED_ONLY = 717u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_BOTH_FRESH_WINS = 718u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_FRESH_METADATA_REJECT = 719u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_AGED_METADATA_REJECT = 720u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_CHOSEN_FRESH = 721u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_CHOSEN_AGED = 722u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_CHOSEN_SELECTED = 723u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_CHOSEN_RETAINED = 724u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_CHOSEN_IDENTITY_SOURCE = 725u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_CHOSEN_MAPPED_SOURCE = 726u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_CHOSEN_ONE_SEGMENT = 727u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_CHOSEN_TWO_SEGMENT = 728u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_AGED_AGE_ONE = 729u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_AGED_AGE_TWO = 730u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_AGED_AGE_THREE = 731u;
public static const uint PATH_SHIFT_DIAGNOSTIC_COUNTER_COUNT = 732u;
public static const uint PATH_SHIFT_DIAGNOSTIC_PAIR_MASK = 3u;
public static const uint PATH_SHIFT_DIAGNOSTIC_MERGE_PAIR_OFFSET =
        PATH_SPATIAL_DIAGNOSTIC_PAIR_CAPACITY * 2u;
public static const uint PATH_SHIFT_DIAGNOSTIC_SELECTION_PAIR_OFFSET =
        PATH_SPATIAL_DIAGNOSTIC_PAIR_CAPACITY * 3u;
public static const uint PATH_GENERATION_MASK = 0x00FFFFFFu;
public static const float PATH_MAX_FOOTPRINT_RATIO = 4.0;
public static const uint PATH_SPATIAL_SAMPLE_COUNT = 8u;
public static const float PATH_SPATIAL_RADIUS = 16.0;
public static const float PATH_SPATIAL_NORMAL_COS = 0.85;
public static const float PATH_SPATIAL_RELATIVE_DEPTH = 0.10;
public static const float PATH_TEMPORAL_MAX_SOURCE_COUNT = 8.0;
public static const float PATH_IDENTITY_SHIFT_JACOBIAN = 1.0;

// Overflow-stable form of mergeWeight / (currentWeightSum + mergeWeight). Inputs are expected to
// be finite and non-negative. This ratio is deliberately independent of the stored weight-sum cap.
public float pathStableSelectionProbability(float currentWeightSum, float mergeWeight)
{
    if (mergeWeight == 0.0) return 0.0;
    if (currentWeightSum <= mergeWeight) {
        return 1.0 / (1.0 + currentWeightSum / mergeWeight);
    }
    float ratio = mergeWeight / currentWeightSum;
    return ratio / (1.0 + ratio);
}

// Exact register-only comparison for the sample lanes of a reservoir. The weight lane is excluded
// because the post-selection gate deliberately replaces it after copying or retaining the sample.
public bool pathReservoirSampleMetadataMatches(PathReservoir left, PathReservoir right)
{
    return all(asuint(left.sampleRadianceTarget) == asuint(right.sampleRadianceTarget))
            && all(asuint(left.proposalShift) == asuint(right.proposalShift))
            && all(asuint(left.proposalComponents) == asuint(right.proposalComponents))
            && all(asuint(left.canonicalRadianceTarget)
                    == asuint(right.canonicalRadianceTarget))
            && all(left.identity == right.identity)
            && all(left.replay == right.replay)
            && all(left.replayControl == right.replayControl)
            && all(asuint(left.reconnectionVertex) == asuint(right.reconnectionVertex))
            && all(asuint(left.reconnectionNormalPdf)
                    == asuint(right.reconnectionNormalPdf))
            && all(asuint(left.reconnectionThroughput)
                    == asuint(right.reconnectionThroughput));
}

public bool pathReservoirSampleMetadataReady(PathReservoir reservoir)
{
    uint packedEvents = asuint(reservoir.proposalComponents.w);
    float expectedSampleTarget = pathLuminance(reservoir.sampleRadianceTarget.xyz);
    float expectedCanonicalTarget = pathLuminance(reservoir.canonicalRadianceTarget.xyz);
    float sampleTolerance = max(1.0e-5, expectedSampleTarget * 1.0e-4);
    float canonicalTolerance = max(1.0e-5, expectedCanonicalTarget * 1.0e-4);
    return all(isfinite(reservoir.sampleRadianceTarget))
            && all(reservoir.sampleRadianceTarget >= float4(0.0))
            && reservoir.sampleRadianceTarget.w > 0.0
            && abs(expectedSampleTarget - reservoir.sampleRadianceTarget.w) <= sampleTolerance
            && all(isfinite(reservoir.proposalShift.xyz))
            && all(reservoir.proposalShift.xyz > float3(0.0))
            && all(isfinite(reservoir.proposalComponents.xyz))
            && all(reservoir.proposalComponents.xyz >= float3(0.0))
            && (packedEvents & PATH_PROPOSAL_CANONICAL_VALID) != 0u
            && (packedEvents & PATH_PROPOSAL_RESERVED_MASK) == 0u
            && all(isfinite(reservoir.canonicalRadianceTarget))
            && all(reservoir.canonicalRadianceTarget >= float4(0.0))
            && abs(expectedCanonicalTarget - reservoir.canonicalRadianceTarget.w)
                    <= canonicalTolerance
            && reservoir.replayControl.x == PATH_REPLAY_VERSION
            && reservoir.replayControl.y >= 1u
            && reservoir.replayControl.y <= MAX_PATH_SEGMENTS
            && asuint(reservoir.proposalShift.w) == reservoir.replayControl.z
            && all(isfinite(reservoir.reconnectionVertex.xyz))
            && all(isfinite(reservoir.reconnectionNormalPdf))
            && all(isfinite(reservoir.reconnectionThroughput.xyz))
            && all(reservoir.reconnectionThroughput.xyz >= float3(0.0))
            && pathMappingControlValid(reservoir);
}

public static const uint PATH_TRANSPORT_DIFFUSE = 1u;
public static const uint PATH_TRANSPORT_GLOSSY = 2u;
public static const uint PATH_TRANSPORT_DELTA = 4u;
public static const uint PATH_TRANSPORT_TRANSMISSION = 8u;
// Increment whenever replay metadata or RNG interpretation changes. Temporal reuse must reject
// reservoirs produced by a different contract instead of trying to replay an incompatible path.
public static const uint PATH_REPLAY_VERSION = 10u;
public static const uint PATH_RECONNECTION_VALID = 1u;
public static const uint PATH_RECONNECTION_DEPTH_SHIFT = 1u;
public static const uint PATH_RECONNECTION_EVENT_SHIFT = 5u;
public static const uint PATH_RECONNECTION_EVENT_MASK = 0x7u << PATH_RECONNECTION_EVENT_SHIFT;
// Stored in reconnectionThroughput.w. The first persistent spatial contract supports exactly one
// diffuse receiver-to-second-hit mapping. A mapped record cannot become another spatial source until
// mapping composition has its own descriptor and Jacobian reference.
public static const uint PATH_MAPPING_IDENTITY = 0u;
public static const uint PATH_MAPPING_DIFFUSE_RECONNECTION = 1u;
public static const uint PATH_MAPPING_KIND_MASK = 0xFu;
public static const uint PATH_MAPPING_RESERVED_MASK = 0xFFFFFFF0u;
public static const uint PATH_RECONNECTION_EVENT_NONE = 0u;
public static const uint PATH_RECONNECTION_EVENT_DIFFUSE = 1u;
public static const uint PATH_RECONNECTION_EVENT_GLOSSY = 2u;
public static const uint PATH_RECONNECTION_EVENT_DELTA = 3u;
public static const uint PATH_RECONNECTION_EVENT_TRANSMISSION = 4u;
public static const uint PATH_CANONICAL_MODE_FLAG = 16u;
public static const uint PATH_MAPPING_REPLAY_PASS_FLAG = 32u;
public static const uint PATH_CROSS_FRAME_MAPPING_REPLAY_PASS_FLAG = 64u;
public static const uint PATH_GUIDE_SCRATCH_VALIDATE_PASS_FLAG = 128u;
public static const uint PATH_GUIDE_PREVIOUS_REPLAY_PASS_FLAG = 256u;
public static const uint PATH_GUIDE_PREVIOUS_AVAILABLE_FLAG = 512u;
public static const uint PATH_GUIDE_BRANCH_PREVIOUS_AVAILABLE_FLAG = 1024u;
public static const uint PATH_GUIDE_BRANCH_SCRATCH_VALIDATE_PASS_FLAG = 2048u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_RETENTION_PASS_FLAG = 4096u;
public static const uint PATH_GUIDE_BRANCH_AGE_REPLAY_PASS_FLAG = 8192u;
public static const uint PATH_GUIDE_BRANCH_AGED_STORAGE_VALIDATE_PASS_FLAG = 16384u;
public static const uint PATH_GUIDE_BRANCH_RECEIVER_OWNER_VALIDATE_PASS_FLAG = 32768u;
public static const uint PATH_GUIDE_BRANCH_WINNER_STORAGE_VALIDATE_PASS_FLAG = 65536u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PREVIOUS_AVAILABLE_FLAG = 131072u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PREVIOUS_REPLAY_PASS_FLAG = 262144u;
public static const uint PATH_GUIDE_BRANCH_WINNER_PAIR_STORAGE_VALIDATE_PASS_FLAG = 524288u;
public static const uint PATH_GUIDE_BRANCH_WINNER_CANDIDATE_OWNER_VALIDATE_PASS_FLAG = 1048576u;
public static const uint PATH_GUIDE_BRANCH_CANDIDATE_ARBITRATION_PASS_FLAG = 2097152u;
// replayControl.y == 0 keeps a terminal sentinel outside the valid replay-segment domain. The
// magic prevents a cleared or partially written record from being mistaken for an intentional
// sample-copy terminal state.
public static const uint PATH_GUIDE_SCRATCH_SENTINEL_MAGIC = 0x47534352u;
public static const uint PATH_PROPOSAL_CANONICAL_VALID = 1u << 30u;
public static const uint PATH_PROPOSAL_RESERVED_MASK = 1u << 31u;
// Debug-view 13 admission states. These numeric values are mirrored by
// RtPathCanonicalReference so shader and CPU diagnostics stay deterministic.
public static const uint PATH_CANONICAL_ACCEPTED = 0u;
public static const uint PATH_CANONICAL_NO_ENDPOINT = 1u;
public static const uint PATH_CANONICAL_INVALID_RADIANCE = 2u;
public static const uint PATH_CANONICAL_ZERO_TARGET = 3u;
public static const uint PATH_CANONICAL_INVALID_CONTINUATION_PDF = 4u;
public static const uint PATH_CANONICAL_INVALID_ROULETTE_PDF = 5u;
public static const uint PATH_CANONICAL_INVALID_PROPOSAL = 6u;

// Replay diagnostic mismatch bits. A pixel may expose more than one independent mismatch; the
// debug color adds one channel per category instead of hiding the root cause behind one enum.
public static const uint PATH_REPLAY_EMPTY = 1u << 0u;
public static const uint PATH_REPLAY_ABI_MISMATCH = 1u << 1u;
public static const uint PATH_REPLAY_SEGMENT_MISMATCH = 1u << 2u;
public static const uint PATH_REPLAY_PATH_STATE_MISMATCH = 1u << 3u;
public static const uint PATH_REPLAY_PROPOSAL_STATE_MISMATCH = 1u << 4u;
public static const uint PATH_REPLAY_TOPOLOGY_MISMATCH = 1u << 5u;
public static const uint PATH_REPLAY_ENDPOINT_MISMATCH = 1u << 6u;
public static const uint PATH_REPLAY_PROPOSAL_MISMATCH = 1u << 7u;
public static const uint PATH_REPLAY_METADATA_MISMATCH = 1u << 8u;

// Temporal merge diagnostic bits. The first five bits explain a rejected history sample;
// accepted merges add MERGED and optionally HISTORY_SELECTED.
public static const uint PATH_TEMPORAL_CURRENT_EMPTY = 1u << 0u;
public static const uint PATH_TEMPORAL_HISTORY_EMPTY = 1u << 1u;
public static const uint PATH_TEMPORAL_REPLAY_REJECT = 1u << 2u;
public static const uint PATH_TEMPORAL_COMPATIBILITY_REJECT = 1u << 3u;
public static const uint PATH_TEMPORAL_FOOTPRINT_REJECT = 1u << 4u;
public static const uint PATH_TEMPORAL_MERGED = 1u << 5u;
public static const uint PATH_TEMPORAL_HISTORY_SELECTED = 1u << 6u;

public struct PathTraceResult {
    public float3 radiance;
    public uint depth;
    public uint topologyKey;
    public uint replaySeed;
    public uint replayState;
    public uint proposalReplaySeed;
    public uint proposalReplayState;
    public uint transportMask;
    public float footprint;
    // Proposal components are diagnostic until the radiance candidate is split into canonical
    // contribution paths; the current candidate still sums NEE and continuation radiance.
    public float lightProposalPdf;
    public uint lightProposalEvents;
    public float continuationProposalPdf;
    public float rouletteProposalPdf;
    public uint continuationProposalEvents;
    public uint rouletteProposalEvents;
    public float3 canonicalRadiance;
    public uint canonicalValid;
    public float canonicalContinuationProposalPdf;
    public float canonicalRouletteProposalPdf;
    public uint canonicalContinuationProposalEvents;
    public uint canonicalRouletteProposalEvents;
    public float3 reconnectionVertex;
    public uint reconnectionValid;
    public uint reconnectionDepth;
    public float3 reconnectionNormal;
    public float reconnectionSourcePdf;
    public uint reconnectionEventKind;
    public float3 reconnectionThroughput;
};

// A complete screen-owned sample can contain both continuation records emitted by Pass A. Keep its
// persistent replay inputs separate from the per-segment trace result so later replay/shift code has an
// explicit contract and does not need to reinterpret a transient queue record.
public struct PathCandidate {
    public float3 radiance;
    public uint depth;
    public uint topologyKey;
    public uint4 replaySeeds;
    public uint replayState;
    public uint proposalReplayState;
    public uint replaySegmentCount;
    public uint transportMask;
    public float footprint;
    public float lightProposalPdf;
    public uint lightProposalEvents;
    public float continuationProposalPdf;
    public float rouletteProposalPdf;
    public uint continuationProposalEvents;
    public uint rouletteProposalEvents;
    public float3 canonicalRadiance;
    public uint canonicalValid;
    public float canonicalContinuationProposalPdf;
    public float canonicalRouletteProposalPdf;
    public uint canonicalContinuationProposalEvents;
    public uint canonicalRouletteProposalEvents;
    public float3 reconnectionVertex;
    public uint reconnectionValid;
    public uint reconnectionDepth;
    public float3 reconnectionNormal;
    public float reconnectionSourcePdf;
    public uint reconnectionEventKind;
    public float3 reconnectionThroughput;
};

// Aggregate returned by a seeded replay of the same queue chain used by the selected candidate.
// Keeping this shape separate from PathCandidate makes the comparison explicit and prevents the
// replay diagnostic from accidentally feeding a second reservoir update.
public struct PathReplayAggregate {
    public float3 radiance;
    public uint depth;
    public uint topologyKey;
    public uint replayState;
    public uint proposalReplayState;
    public uint replaySegmentCount;
    public uint transportMask;
    public float footprint;
    public float lightProposalPdf;
    public uint lightProposalEvents;
    public float continuationProposalPdf;
    public float rouletteProposalPdf;
    public uint continuationProposalEvents;
    public uint rouletteProposalEvents;
    public float3 canonicalRadiance;
    public uint canonicalValid;
    public float canonicalContinuationProposalPdf;
    public float canonicalRouletteProposalPdf;
    public uint canonicalContinuationProposalEvents;
    public uint canonicalRouletteProposalEvents;
    public float3 reconnectionVertex;
    public uint reconnectionValid;
    public uint reconnectionDepth;
    public float3 reconnectionNormal;
    public float reconnectionSourcePdf;
    public uint reconnectionEventKind;
    public float3 reconnectionThroughput;
};

public PathReplayAggregate pathReplayAggregateEmpty() {
    PathReplayAggregate aggregate;
    aggregate.radiance = float3(0.0);
    aggregate.depth = 0u;
    aggregate.topologyKey = pathHash(0x7F4A7C15u);
    aggregate.replayState = 0u;
    aggregate.proposalReplayState = 0u;
    aggregate.replaySegmentCount = 0u;
    aggregate.transportMask = 0u;
    aggregate.footprint = 1.0e-5;
    aggregate.lightProposalPdf = 1.0;
    aggregate.lightProposalEvents = 0u;
    aggregate.continuationProposalPdf = 1.0;
    aggregate.rouletteProposalPdf = 1.0;
    aggregate.continuationProposalEvents = 0u;
    aggregate.rouletteProposalEvents = 0u;
    aggregate.canonicalRadiance = float3(0.0);
    aggregate.canonicalValid = 0u;
    aggregate.canonicalContinuationProposalPdf = 1.0;
    aggregate.canonicalRouletteProposalPdf = 1.0;
    aggregate.canonicalContinuationProposalEvents = 0u;
    aggregate.canonicalRouletteProposalEvents = 0u;
    aggregate.reconnectionVertex = float3(0.0);
    aggregate.reconnectionValid = 0u;
    aggregate.reconnectionDepth = 0u;
    aggregate.reconnectionNormal = float3(0.0);
    aggregate.reconnectionSourcePdf = 0.0;
    aggregate.reconnectionEventKind = PATH_RECONNECTION_EVENT_NONE;
    aggregate.reconnectionThroughput = float3(0.0);
    return aggregate;
}

public void pathReplayAccumulate(inout PathReplayAggregate aggregate,
                                 PathTraceResult traced, uint leaf) {
    aggregate.replaySegmentCount = leaf + 1u;
    aggregate.radiance += traced.radiance;
    aggregate.depth = max(aggregate.depth, traced.depth);
    aggregate.topologyKey = pathTopologyStep(aggregate.topologyKey,
            traced.topologyKey ^ leaf * 668265263u);
    aggregate.replayState = pathHash(
            aggregate.replayState ^ traced.replayState ^ leaf * 374761393u);
    aggregate.proposalReplayState = pathHash(
            aggregate.proposalReplayState ^ traced.proposalReplayState ^ leaf * 1103515245u);
    aggregate.transportMask |= traced.transportMask;
    aggregate.footprint = max(aggregate.footprint, traced.footprint);
    aggregate.lightProposalPdf = pathCombineProposal(
            aggregate.lightProposalPdf, traced.lightProposalPdf);
    aggregate.lightProposalEvents += traced.lightProposalEvents;
    aggregate.continuationProposalPdf = pathCombineProposal(
            aggregate.continuationProposalPdf, traced.continuationProposalPdf);
    aggregate.continuationProposalEvents += traced.continuationProposalEvents;
    aggregate.rouletteProposalPdf = pathCombineProposal(
            aggregate.rouletteProposalPdf, traced.rouletteProposalPdf);
    aggregate.rouletteProposalEvents += traced.rouletteProposalEvents;
    if (aggregate.canonicalValid == 0u && traced.canonicalValid != 0u) {
        aggregate.canonicalRadiance = traced.canonicalRadiance;
        aggregate.canonicalValid = 1u;
        aggregate.canonicalContinuationProposalPdf = traced.canonicalContinuationProposalPdf;
        aggregate.canonicalContinuationProposalEvents = traced.canonicalContinuationProposalEvents;
        aggregate.canonicalRouletteProposalPdf = traced.canonicalRouletteProposalPdf;
        aggregate.canonicalRouletteProposalEvents = traced.canonicalRouletteProposalEvents;
        aggregate.reconnectionVertex = traced.reconnectionVertex;
        aggregate.reconnectionValid = traced.reconnectionValid;
        aggregate.reconnectionDepth = traced.reconnectionDepth;
        aggregate.reconnectionNormal = traced.reconnectionNormal;
        aggregate.reconnectionSourcePdf = traced.reconnectionSourcePdf;
        aggregate.reconnectionEventKind = traced.reconnectionEventKind;
        aggregate.reconnectionThroughput = traced.reconnectionThroughput;
    }
}

public bool pathReplayFloatMatches(float expected, float actual) {
    float tolerance = max(1.0e-5, max(abs(expected), abs(actual)) * 1.0e-4);
    return isfinite(expected) && isfinite(actual) && abs(expected - actual) <= tolerance;
}

public bool pathReplayColorMatches(float3 expected, float3 actual) {
    return all(pathReplayFloatMatches(expected.r, actual.r)
            && pathReplayFloatMatches(expected.g, actual.g)
            && pathReplayFloatMatches(expected.b, actual.b));
}

public bool pathReplayPositionMatches(float3 expected, float3 actual) {
    return all(pathReplayFloatMatches(expected.x, actual.x)
            && pathReplayFloatMatches(expected.y, actual.y)
            && pathReplayFloatMatches(expected.z, actual.z));
}

public uint pathReplayCompare(PathReservoir reservoir, PathReplayAggregate replay) {
    uint mismatch = 0u;
    uint segmentCount = reservoir.replayControl.y;
    if (reservoir.replayControl.x != PATH_REPLAY_VERSION
            || segmentCount < 1u || segmentCount > MAX_PATH_SEGMENTS
            || (segmentCount < MAX_PATH_SEGMENTS && any(reservoir.replay.zw != uint2(0u)))
            || !pathMappingControlValid(reservoir)
            // Generic exact replay reconstructs an identity path. A spatially mapped record must
            // pass through the future receiver-aware mapping replay before it can be admitted.
            || pathMappingKind(reservoir) != PATH_MAPPING_IDENTITY) {
        mismatch |= PATH_REPLAY_ABI_MISMATCH;
    }
    if (replay.replaySegmentCount != segmentCount) {
        mismatch |= PATH_REPLAY_SEGMENT_MISMATCH;
    }
    if (replay.replayState != reservoir.replayControl.z) {
        mismatch |= PATH_REPLAY_PATH_STATE_MISMATCH;
    }
    if (replay.proposalReplayState != reservoir.replayControl.w) {
        mismatch |= PATH_REPLAY_PROPOSAL_STATE_MISMATCH;
    }
    if (replay.topologyKey != reservoir.identity.z) {
        mismatch |= PATH_REPLAY_TOPOLOGY_MISMATCH;
    }
    if (replay.canonicalValid == 0u
            || !pathReplayColorMatches(replay.canonicalRadiance,
                    reservoir.canonicalRadianceTarget.xyz)
            || !pathReplayFloatMatches(pathLuminance(replay.canonicalRadiance),
                    reservoir.canonicalRadianceTarget.w)) {
        mismatch |= PATH_REPLAY_ENDPOINT_MISMATCH;
    }
    uint packedEvents = asuint(reservoir.proposalComponents.w);
    uint expectedCanonicalValid = (packedEvents & PATH_PROPOSAL_CANONICAL_VALID) != 0u ? 1u : 0u;
    if (!pathReplayFloatMatches(replay.lightProposalPdf, reservoir.proposalComponents.x)
            || !pathReplayFloatMatches(replay.canonicalContinuationProposalPdf,
                    reservoir.proposalComponents.y)
            || !pathReplayFloatMatches(replay.canonicalRouletteProposalPdf,
                    reservoir.proposalComponents.z)
            || ((replay.canonicalValid != 0u ? 1u : 0u) != expectedCanonicalValid)
            || ((packedEvents & 1023u)
                    != min(replay.lightProposalEvents, 1023u))
            || (((packedEvents >> 10u) & 1023u)
                    != min(replay.canonicalContinuationProposalEvents, 1023u))
            || (((packedEvents >> 20u) & 1023u)
                    != min(replay.canonicalRouletteProposalEvents, 1023u))
            || (packedEvents & PATH_PROPOSAL_RESERVED_MASK) != 0u) {
        mismatch |= PATH_REPLAY_PROPOSAL_MISMATCH;
    }
    uint storedMetadata = reservoir.identity.w;
    uint replayMetadata = pathPackMetadata(storedMetadata & PATH_GENERATION_MASK,
            replay.depth, replay.transportMask);
    if (replayMetadata != storedMetadata) {
        mismatch |= PATH_REPLAY_METADATA_MISMATCH;
    }
    uint packedReconnection = asuint(reservoir.reconnectionVertex.w);
    bool storedReconnectionValid = (packedReconnection & PATH_RECONNECTION_VALID) != 0u;
    uint storedReconnectionDepth =
            (packedReconnection >> PATH_RECONNECTION_DEPTH_SHIFT) & 0xFu;
    uint storedReconnectionEvent = (packedReconnection & PATH_RECONNECTION_EVENT_MASK)
            >> PATH_RECONNECTION_EVENT_SHIFT;
    if (storedReconnectionValid != (replay.reconnectionValid != 0u)
            || (storedReconnectionValid
                    && (!pathReplayPositionMatches(replay.reconnectionVertex,
                            reservoir.reconnectionVertex.xyz)
                        || replay.reconnectionDepth != storedReconnectionDepth
                        || replay.reconnectionEventKind != storedReconnectionEvent
                        || !pathReplayPositionMatches(replay.reconnectionNormal,
                                reservoir.reconnectionNormalPdf.xyz)
                        || !pathReplayFloatMatches(replay.reconnectionSourcePdf,
                                reservoir.reconnectionNormalPdf.w)
                        || !pathReplayColorMatches(replay.reconnectionThroughput,
                                reservoir.reconnectionThroughput.xyz)
                        || asuint(reservoir.reconnectionThroughput.w) != 0u))) {
        mismatch |= PATH_REPLAY_METADATA_MISMATCH;
    }
    return mismatch;
}

// Validates the canonical source portion of a one-hop mapped record. The mapped endpoint,
// directional PDF, and first-edge throughput are receiver-side values, so they are deliberately
// checked later by the receiver-aware replay pass rather than compared with the source aggregate.
public bool pathMappingSourceReplayMatches(
        PathReservoir mapped, PathReplayAggregate replay) {
    uint segmentCount = mapped.replayControl.y;
    if (mapped.replayControl.x != PATH_REPLAY_VERSION
            || segmentCount < 1u || segmentCount > MAX_PATH_SEGMENTS
            || (segmentCount < MAX_PATH_SEGMENTS && any(mapped.replay.zw != uint2(0u)))
            || !pathMappingControlValid(mapped)
            || pathMappingKind(mapped) != PATH_MAPPING_DIFFUSE_RECONNECTION
            || asuint(mapped.proposalShift.w) != mapped.replayControl.z
            || replay.replaySegmentCount != segmentCount
            || replay.replayState != mapped.replayControl.z
            || replay.proposalReplayState != mapped.replayControl.w
            || replay.topologyKey != mapped.identity.z
            || replay.canonicalValid == 0u
            || !pathReplayFloatMatches(replay.footprint, mapped.proposalShift.z)) {
        return false;
    }

    uint packedEvents = asuint(mapped.proposalComponents.w);
    if (!pathReplayFloatMatches(replay.lightProposalPdf, mapped.proposalComponents.x)
            || !pathReplayFloatMatches(replay.canonicalContinuationProposalPdf,
                    mapped.proposalComponents.y)
            || !pathReplayFloatMatches(replay.canonicalRouletteProposalPdf,
                    mapped.proposalComponents.z)
            || (packedEvents & PATH_PROPOSAL_CANONICAL_VALID) == 0u
            || ((packedEvents & 1023u) != min(replay.lightProposalEvents, 1023u))
            || (((packedEvents >> 10u) & 1023u)
                    != min(replay.canonicalContinuationProposalEvents, 1023u))
            || (((packedEvents >> 20u) & 1023u)
                    != min(replay.canonicalRouletteProposalEvents, 1023u))
            || (packedEvents & PATH_PROPOSAL_RESERVED_MASK) != 0u) {
        return false;
    }

    uint storedMetadata = mapped.identity.w;
    if (pathPackMetadata(storedMetadata & PATH_GENERATION_MASK,
            replay.depth, replay.transportMask) != storedMetadata) {
        return false;
    }

    uint packedReconnection = asuint(mapped.reconnectionVertex.w);
    bool storedReconnectionValid = (packedReconnection & PATH_RECONNECTION_VALID) != 0u;
    uint storedReconnectionDepth =
            (packedReconnection >> PATH_RECONNECTION_DEPTH_SHIFT) & 0xFu;
    uint storedReconnectionEvent = pathReconnectionEvent(packedReconnection);
    return storedReconnectionValid
            && storedReconnectionDepth == 1u
            && storedReconnectionEvent == PATH_RECONNECTION_EVENT_DIFFUSE
            && replay.reconnectionValid != 0u
            && replay.reconnectionDepth == storedReconnectionDepth
            && replay.reconnectionEventKind == storedReconnectionEvent
            && pathReplayPositionMatches(replay.reconnectionVertex,
                    mapped.reconnectionVertex.xyz)
            && pathReplayPositionMatches(replay.reconnectionNormal,
                    mapped.reconnectionNormalPdf.xyz)
            && isfinite(replay.reconnectionSourcePdf)
            && replay.reconnectionSourcePdf > 0.0
            && all(isfinite(replay.reconnectionThroughput))
            && all(replay.reconnectionThroughput >= float3(0.0))
            && isfinite(mapped.reconnectionNormalPdf.w)
            && mapped.reconnectionNormalPdf.w > 0.0
            && all(isfinite(mapped.reconnectionThroughput.xyz))
            && all(mapped.reconnectionThroughput.xyz >= float3(0.0));
}

public float3 pathReplayDebugColor(uint mismatch) {
    if ((mismatch & PATH_REPLAY_EMPTY) != 0u) {
        return float3(0.0);
    }
    if (mismatch == 0u) {
        return float3(0.05, 0.95, 0.20); // exact seeded replay
    }
    float3 color = float3(0.0);
    if ((mismatch & PATH_REPLAY_ABI_MISMATCH) != 0u) color += float3(0.05, 0.85, 0.95);
    if ((mismatch & PATH_REPLAY_SEGMENT_MISMATCH) != 0u) color += float3(0.55, 0.10, 0.95);
    if ((mismatch & PATH_REPLAY_PATH_STATE_MISMATCH) != 0u) color += float3(0.95, 0.05, 0.05);
    if ((mismatch & PATH_REPLAY_PROPOSAL_STATE_MISMATCH) != 0u) color += float3(0.95, 0.30, 0.04);
    if ((mismatch & PATH_REPLAY_TOPOLOGY_MISMATCH) != 0u) color += float3(0.15, 0.35, 0.95);
    if ((mismatch & PATH_REPLAY_ENDPOINT_MISMATCH) != 0u) color += float3(1.00, 0.12, 0.55);
    if ((mismatch & PATH_REPLAY_PROPOSAL_MISMATCH) != 0u) color += float3(0.95, 0.72, 0.04);
    if ((mismatch & PATH_REPLAY_METADATA_MISMATCH) != 0u) color += float3(0.80, 0.80, 0.80);
    return min(color, float3(1.0));
}

public float pathLuminance(float3 color) {
    return dot(color, float3(0.2126, 0.7152, 0.0722));
}

public PathReservoir pathReservoirEmpty() {
    PathReservoir reservoir;
    reservoir.sampleRadianceTarget = float4(0.0);
    reservoir.proposalShift = float4(1.0, 1.0, 1.0, 0.0);
    reservoir.weights = float4(0.0);
    reservoir.identity = uint4(0u);
    reservoir.replay = uint4(0u);
    reservoir.replayControl = uint4(0u);
    reservoir.reconnectionVertex = float4(0.0);
    reservoir.reconnectionNormalPdf = float4(0.0);
    reservoir.reconnectionThroughput = float4(0.0);
    reservoir.proposalComponents = float4(0.0, 0.0, 0.0, 0.0);
    reservoir.canonicalRadianceTarget = float4(0.0);
    return reservoir;
}

public uint pathHash(uint value) {
    uint state = value;
    return pcg(state);
}

public uint pathTopologyStep(uint topology, uint eventToken) {
    return pathHash(topology ^ eventToken * 2246822519u + 3266489917u);
}

public uint pathPackMetadata(uint generation, uint depth, uint transportMask) {
    return (generation & PATH_GENERATION_MASK)
            | ((min(depth, 15u) & 0xFu) << 24u)
            | ((transportMask & 0xFu) << 28u);
}

public float pathCombineProposal(float left, float right) {
    if (left <= 0.0 || right <= 0.0) return 0.0;
    float combined = left * right;
    return combined > 0.0 ? clamp(combined, 1.0e-30, 1.0e30) : 0.0;
}

public void pathAccumulateProposal(inout float product, inout uint events, float factor) {
    product = pathCombineProposal(product, factor);
    events++;
}

public uint pathPackProposalEvents(uint lightEvents, uint continuationEvents, uint rouletteEvents,
                                   bool canonicalValid) {
    return min(lightEvents, 1023u)
            | (min(continuationEvents, 1023u) << 10u)
            | (min(rouletteEvents, 1023u) << 20u)
            | (canonicalValid ? PATH_PROPOSAL_CANONICAL_VALID : 0u);
}

public uint pathPackReconnectionMetadata(uint depth, uint eventKind, bool valid) {
    return (valid ? PATH_RECONNECTION_VALID : 0u)
            | ((min(depth, 15u) & 0xFu) << PATH_RECONNECTION_DEPTH_SHIFT)
            | ((eventKind & 0x7u) << PATH_RECONNECTION_EVENT_SHIFT);
}

public uint pathReconnectionEvent(uint packedMetadata) {
    return (packedMetadata & PATH_RECONNECTION_EVENT_MASK) >> PATH_RECONNECTION_EVENT_SHIFT;
}

public uint pathMappingKind(PathReservoir reservoir) {
    return asuint(reservoir.reconnectionThroughput.w) & PATH_MAPPING_KIND_MASK;
}

public bool pathMappingControlValid(PathReservoir reservoir) {
    uint control = asuint(reservoir.reconnectionThroughput.w);
    uint kind = control & PATH_MAPPING_KIND_MASK;
    return (control & PATH_MAPPING_RESERVED_MASK) == 0u
            && (kind == PATH_MAPPING_IDENTITY
                    || kind == PATH_MAPPING_DIFFUSE_RECONNECTION);
}

public bool pathReconnectionContinuous(uint eventKind) {
    return eventKind == PATH_RECONNECTION_EVENT_DIFFUSE
            || eventKind == PATH_RECONNECTION_EVENT_GLOSSY;
}

public bool pathCanonicalEndpointConsistent(PathReservoir reservoir) {
    uint packedEvents = asuint(reservoir.proposalComponents.w);
    if ((packedEvents & PATH_PROPOSAL_CANONICAL_VALID) == 0u
            || (packedEvents & PATH_PROPOSAL_RESERVED_MASK) != 0u) {
        return false;
    }
    float3 radiance = reservoir.canonicalRadianceTarget.xyz;
    float storedTarget = reservoir.canonicalRadianceTarget.w;
    if (!all(radiance >= float3(0.0)) || !(storedTarget >= 0.0)) {
        return false;
    }
    float expectedTarget = pathLuminance(radiance);
    float tolerance = max(1.0e-5, expectedTarget * 1.0e-4);
    return abs(expectedTarget - storedTarget) <= tolerance;
}

public uint pathMetadataDepth(uint metadata) {
    return (metadata >> 24u) & 0xFu;
}

public uint pathMetadataTransport(uint metadata) {
    return (metadata >> 28u) & 0xFu;
}

public uint pathCanonicalCandidateStatus(PathCandidate candidate) {
    if (candidate.canonicalValid == 0u) {
        return PATH_CANONICAL_NO_ENDPOINT;
    }
    if (!all(isfinite(candidate.canonicalRadiance))
            || !all(candidate.canonicalRadiance >= float3(0.0))) {
        return PATH_CANONICAL_INVALID_RADIANCE;
    }
    float target = pathLuminance(candidate.canonicalRadiance);
    if (!isfinite(target) || target < 0.0) {
        return PATH_CANONICAL_INVALID_RADIANCE;
    }
    if (target == 0.0) {
        return PATH_CANONICAL_ZERO_TARGET;
    }
    if (!isfinite(candidate.canonicalContinuationProposalPdf)
            || candidate.canonicalContinuationProposalPdf <= 0.0) {
        return PATH_CANONICAL_INVALID_CONTINUATION_PDF;
    }
    if (!isfinite(candidate.canonicalRouletteProposalPdf)
            || candidate.canonicalRouletteProposalPdf <= 0.0) {
        return PATH_CANONICAL_INVALID_ROULETTE_PDF;
    }
    float proposalDensity = pathCombineProposal(candidate.canonicalContinuationProposalPdf,
            candidate.canonicalRouletteProposalPdf);
    float candidateWeight = target / proposalDensity;
    if (!isfinite(proposalDensity) || proposalDensity <= 0.0
            || !isfinite(candidateWeight) || candidateWeight <= 0.0) {
        return PATH_CANONICAL_INVALID_PROPOSAL;
    }
    return PATH_CANONICAL_ACCEPTED;
}

public float3 pathCanonicalCandidateDebugColor(uint status) {
    if (status == PATH_CANONICAL_ACCEPTED) {
        return float3(0.05, 0.95, 0.20); // admitted
    }
    if (status == PATH_CANONICAL_NO_ENDPOINT) {
        return float3(1.00, 0.12, 0.55); // no sky/emissive endpoint
    }
    if (status == PATH_CANONICAL_INVALID_RADIANCE) {
        return float3(0.95, 0.05, 0.05); // non-finite or negative radiance/target
    }
    if (status == PATH_CANONICAL_ZERO_TARGET) {
        return float3(0.10, 0.25, 1.00); // valid black/zero-target endpoint
    }
    if (status == PATH_CANONICAL_INVALID_CONTINUATION_PDF) {
        return float3(0.05, 0.85, 0.95); // continuation PDF
    }
    if (status == PATH_CANONICAL_INVALID_ROULETTE_PDF) {
        return float3(0.95, 0.30, 0.04); // roulette PDF
    }
    return float3(0.95, 0.72, 0.04); // combined proposal or importance weight
}

// Initial per-pixel RIS update. The traced path is already an importance-weighted Monte-Carlo
// estimator, so this bootstrap stage uses an effective proposal density of one. A later replay pass
// will replace it with decomposed path PDFs before any temporal or spatial path reuse is allowed.
public uint pathReservoirUpdate(inout PathReservoir reservoir,
                                PathCandidate candidate,
                                uint sampleId,
                                uint pathKey,
                                uint historyGeneration,
                                inout uint selectionSeed,
                                bool canonicalMode) {
    uint canonicalStatus = PATH_CANONICAL_ACCEPTED;
    if (canonicalMode) {
        canonicalStatus = pathCanonicalCandidateStatus(candidate);
        if (canonicalStatus != PATH_CANONICAL_ACCEPTED) {
            return canonicalStatus;
        }
    }
    float3 radiance = canonicalMode
            ? max(candidate.canonicalRadiance, float3(0.0))
            : max(candidate.radiance, float3(0.0));
    float target = max(pathLuminance(radiance), 0.0);
    // The regular capture remains bootstrap-weighted because its candidate still sums multiple
    // estimators. The opt-in canonical debug mode has one endpoint and uses its continuation and
    // roulette products as the first path-only proposal-density contract.
    float proposalDensity = canonicalMode
            ? pathCombineProposal(candidate.canonicalContinuationProposalPdf,
                    candidate.canonicalRouletteProposalPdf)
            : 1.0;
    float candidateWeight = target / proposalDensity;
    float previousWeightSum = reservoir.weights.x;
    float nextWeightSum = min(previousWeightSum + candidateWeight, 1.0e30);
    float nextM = min(reservoir.weights.y + 1.0, 16777216.0);
    bool selectCandidate = candidateWeight > 0.0
            && (previousWeightSum <= 0.0 || rndf(selectionSeed) < candidateWeight / nextWeightSum);

    if (selectCandidate) {
        reservoir.sampleRadianceTarget = float4(radiance, target);
        reservoir.proposalShift = float4(1.0, 1.0,
                max(candidate.footprint, 1.0e-5), asfloat(candidate.replayState));
        reservoir.proposalComponents = float4(
                candidate.lightProposalPdf,
                candidate.canonicalContinuationProposalPdf,
                candidate.canonicalRouletteProposalPdf,
                asfloat(pathPackProposalEvents(candidate.lightProposalEvents,
                        candidate.canonicalContinuationProposalEvents,
                        candidate.canonicalRouletteProposalEvents,
                        candidate.canonicalValid != 0u)));
        float3 canonicalRadiance = max(candidate.canonicalRadiance, float3(0.0));
        float canonicalTarget = candidate.canonicalValid != 0u
                ? max(pathLuminance(canonicalRadiance), 0.0) : 0.0;
        reservoir.canonicalRadianceTarget = float4(canonicalRadiance, canonicalTarget);
        reservoir.identity = uint4(sampleId, pathKey, candidate.topologyKey,
                pathPackMetadata(historyGeneration, candidate.depth, candidate.transportMask));
        // Storage-only replay contract. The current estimator deliberately does not consume this lane;
        // a later path-replay stage will validate the version before reconstructing the selected path.
        reservoir.replay = candidate.replaySeeds;
        reservoir.replayControl = uint4(PATH_REPLAY_VERSION,
                min(candidate.replaySegmentCount, MAX_PATH_SEGMENTS),
                candidate.replayState, candidate.proposalReplayState);
        reservoir.reconnectionVertex = float4(candidate.reconnectionVertex,
                asfloat(pathPackReconnectionMetadata(candidate.reconnectionDepth,
                        candidate.reconnectionEventKind, candidate.reconnectionValid != 0u)));
        reservoir.reconnectionNormalPdf = float4(candidate.reconnectionNormal,
                candidate.reconnectionSourcePdf);
        reservoir.reconnectionThroughput = float4(candidate.reconnectionThroughput, 0.0);
    }

    reservoir.weights.x = nextWeightSum;
    reservoir.weights.y = nextM;
    float selectedTarget = reservoir.sampleRadianceTarget.w;
    reservoir.weights.z = selectedTarget > 0.0 && nextM > 0.0
            ? nextWeightSum / (nextM * selectedTarget) : 0.0;
    reservoir.weights.w = selectedTarget > 0.0 ? min(nextM * 0.25, 1.0) : 0.0;
    return canonicalStatus;
}

public PathCandidate pathCandidateFromReplay(PathReplayAggregate replay,
                                              PathReservoir source) {
    PathCandidate candidate;
    candidate.radiance = replay.radiance;
    candidate.depth = replay.depth;
    candidate.topologyKey = replay.topologyKey;
    candidate.replaySeeds = source.replay;
    candidate.replayState = replay.replayState;
    candidate.proposalReplayState = replay.proposalReplayState;
    candidate.replaySegmentCount = replay.replaySegmentCount;
    candidate.transportMask = replay.transportMask;
    candidate.footprint = replay.footprint;
    candidate.lightProposalPdf = replay.lightProposalPdf;
    candidate.lightProposalEvents = replay.lightProposalEvents;
    candidate.continuationProposalPdf = replay.continuationProposalPdf;
    candidate.rouletteProposalPdf = replay.rouletteProposalPdf;
    candidate.continuationProposalEvents = replay.continuationProposalEvents;
    candidate.rouletteProposalEvents = replay.rouletteProposalEvents;
    candidate.canonicalRadiance = replay.canonicalRadiance;
    candidate.canonicalValid = replay.canonicalValid;
    candidate.canonicalContinuationProposalPdf = replay.canonicalContinuationProposalPdf;
    candidate.canonicalRouletteProposalPdf = replay.canonicalRouletteProposalPdf;
    candidate.canonicalContinuationProposalEvents = replay.canonicalContinuationProposalEvents;
    candidate.canonicalRouletteProposalEvents = replay.canonicalRouletteProposalEvents;
    candidate.reconnectionVertex = replay.reconnectionVertex;
    candidate.reconnectionValid = replay.reconnectionValid;
    candidate.reconnectionDepth = replay.reconnectionDepth;
    candidate.reconnectionNormal = replay.reconnectionNormal;
    candidate.reconnectionSourcePdf = replay.reconnectionSourcePdf;
    candidate.reconnectionEventKind = replay.reconnectionEventKind;
    candidate.reconnectionThroughput = replay.reconnectionThroughput;
    return candidate;
}

public uint pathReservoirTemporalMerge(inout PathReservoir receiver,
                                       PathReservoir source,
                                       PathCandidate shifted,
                                       uint pathKey,
                                       uint historyGeneration,
                                       inout uint selectionSeed) {
    uint status = 0u;
    if (receiver.weights.y <= 0.0 || receiver.sampleRadianceTarget.w <= 0.0) {
        status |= PATH_TEMPORAL_CURRENT_EMPTY;
    }
    if (source.weights.y <= 0.0 || source.sampleRadianceTarget.w <= 0.0
            || source.weights.z <= 0.0 || shifted.canonicalValid == 0u) {
        return status | PATH_TEMPORAL_HISTORY_EMPTY;
    }

    uint sourceMetadata = source.identity.w;
    uint shiftedMetadata = pathPackMetadata(sourceMetadata & PATH_GENERATION_MASK,
            shifted.depth, shifted.transportMask);
    if (shifted.topologyKey != source.identity.z
            || shiftedMetadata != sourceMetadata) {
        return status | PATH_TEMPORAL_COMPATIBILITY_REJECT;
    }
    float sourceFootprint = max(source.proposalShift.z, 1.0e-5);
    float shiftedFootprint = max(shifted.footprint, 1.0e-5);
    float sourceFootprintRatio = max(sourceFootprint, shiftedFootprint)
            / min(sourceFootprint, shiftedFootprint);
    if (sourceFootprintRatio > PATH_MAX_FOOTPRINT_RATIO) {
        return status | PATH_TEMPORAL_FOOTPRINT_REJECT;
    }
    if (receiver.weights.y > 0.0) {
        uint receiverMetadata = receiver.identity.w;
        if (receiver.identity.z != shifted.topologyKey
                || pathMetadataDepth(receiverMetadata) != shifted.depth
                || pathMetadataTransport(receiverMetadata) != (shifted.transportMask & 0xFu)) {
            return status | PATH_TEMPORAL_COMPATIBILITY_REJECT;
        }
        float receiverFootprint = max(receiver.proposalShift.z, 1.0e-5);
        float receiverFootprintRatio = max(receiverFootprint, shiftedFootprint)
                / min(receiverFootprint, shiftedFootprint);
        if (receiverFootprintRatio > PATH_MAX_FOOTPRINT_RATIO) {
            return status | PATH_TEMPORAL_FOOTPRINT_REJECT;
        }
    }

    float sourceCount = min(source.weights.y, PATH_TEMPORAL_MAX_SOURCE_COUNT);
    float mergeTarget = max(pathLuminance(shifted.canonicalRadiance), 0.0);
    float mergeWeight = mergeTarget * source.weights.z * sourceCount
            * PATH_IDENTITY_SHIFT_JACOBIAN;
    float nextWeightSum = min(receiver.weights.x + mergeWeight, 1.0e30);
    float nextM = min(receiver.weights.y + sourceCount, 16777216.0);
    bool selectHistory = mergeWeight > 0.0
            && (receiver.weights.x <= 0.0
                    || rndf(selectionSeed) < mergeWeight / nextWeightSum);
    if (selectHistory) {
        receiver.sampleRadianceTarget = float4(max(shifted.canonicalRadiance, float3(0.0)),
                mergeTarget);
        receiver.proposalShift = float4(1.0, 1.0, shiftedFootprint,
                asfloat(shifted.replayState));
        receiver.proposalComponents = float4(
                shifted.lightProposalPdf,
                shifted.canonicalContinuationProposalPdf,
                shifted.canonicalRouletteProposalPdf,
                asfloat(pathPackProposalEvents(shifted.lightProposalEvents,
                        shifted.canonicalContinuationProposalEvents,
                        shifted.canonicalRouletteProposalEvents,
                        shifted.canonicalValid != 0u)));
        receiver.canonicalRadianceTarget = float4(max(shifted.canonicalRadiance, float3(0.0)),
                mergeTarget);
        receiver.identity = uint4(source.identity.x, pathKey, shifted.topologyKey,
                pathPackMetadata(historyGeneration, shifted.depth, shifted.transportMask));
        receiver.replay = shifted.replaySeeds;
        receiver.replayControl = uint4(PATH_REPLAY_VERSION,
                min(shifted.replaySegmentCount, MAX_PATH_SEGMENTS),
                shifted.replayState, shifted.proposalReplayState);
        receiver.reconnectionVertex = float4(shifted.reconnectionVertex,
                asfloat(pathPackReconnectionMetadata(shifted.reconnectionDepth,
                        shifted.reconnectionEventKind, shifted.reconnectionValid != 0u)));
        receiver.reconnectionNormalPdf = float4(shifted.reconnectionNormal,
                shifted.reconnectionSourcePdf);
        receiver.reconnectionThroughput = float4(shifted.reconnectionThroughput, 0.0);
        status |= PATH_TEMPORAL_HISTORY_SELECTED;
    }
    receiver.weights.x = nextWeightSum;
    receiver.weights.y = nextM;
    float selectedTarget = receiver.sampleRadianceTarget.w;
    receiver.weights.z = selectedTarget > 0.0 && nextM > 0.0
            ? nextWeightSum / (nextM * selectedTarget) : 0.0;
    receiver.weights.w = selectedTarget > 0.0 ? min(nextM * 0.25, 1.0) : 0.0;
    return status | PATH_TEMPORAL_MERGED;
}

public float3 pathTemporalMergeDebugColor(uint status) {
    if ((status & PATH_TEMPORAL_HISTORY_EMPTY) != 0u
            && (status & PATH_TEMPORAL_CURRENT_EMPTY) != 0u) {
        return float3(0.0);
    }
    if ((status & PATH_TEMPORAL_REPLAY_REJECT) != 0u) {
        return float3(0.05, 0.85, 0.95);
    }
    if ((status & PATH_TEMPORAL_FOOTPRINT_REJECT) != 0u) {
        return float3(0.55, 0.10, 0.95);
    }
    if ((status & PATH_TEMPORAL_COMPATIBILITY_REJECT) != 0u) {
        return float3(0.15, 0.35, 0.95);
    }
    if ((status & PATH_TEMPORAL_HISTORY_EMPTY) != 0u) {
        return float3(0.10, 0.10, 0.10);
    }
    if ((status & PATH_TEMPORAL_HISTORY_SELECTED) != 0u) {
        return float3(0.05, 0.95, 0.20);
    }
    if ((status & PATH_TEMPORAL_MERGED) != 0u) {
        return float3(0.20, 0.70, 0.35);
    }
    return float3(0.80, 0.65, 0.05);
}

// Diagnostic channels: red = normalized traced depth, green = selected target luminance,
// blue = transport-class mask. Black means no positive-contribution path was selected.
public float3 pathReservoirDebug(PathReservoir reservoir, uint maxDepth) {
    if (reservoir.weights.y <= 0.0 || reservoir.sampleRadianceTarget.w <= 0.0) {
        return float3(0.0);
    }
    uint metadata = reservoir.identity.w;
    float depth = float((metadata >> 24u) & 0xFu) / float(max(maxDepth, 1u));
    float target = reservoir.sampleRadianceTarget.w;
    float transport = float((metadata >> 28u) & 0xFu) / 15.0;
    return float3(clamp(depth, 0.0, 1.0), target / (1.0 + target), transport);
}
